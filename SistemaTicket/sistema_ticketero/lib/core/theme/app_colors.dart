import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFFE30613);
  static const Color primaryLight = Color(0xFFFF1A1A);
  static const Color primaryDark = Color(0xFFB00510);

  static const Color secondary = Color(0xFF00897B);
  static const Color accent = Color(0xFFFFA726);

  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;
  static const Color border = Color(0xFFE0E0E0);

  static const Color success = Color(0xFF43A047);
  static const Color warning = Color(0xFFFFA000);
  static const Color error = Color(0xFFE53935);
  static const Color info = Color(0xFF1E88E5);

  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textOnPrimary = Colors.white;

  // Colores para tipos de ticket
  static const Color ticketCaja = Color(0xFF1565C0);
  static const Color ticketInfo = Color(0xFF00897B);
  static const Color ticketInsc = Color(0xFFE65100);
  static const Color ticketDoc = Color(0xFF6A1B9A);

  // Colores para estados
  static const Color statusPendiente = Color(0xFFFFA000);
  static const Color statusLlamado = Color(0xFF43A047);
  static const Color statusAtencion = Color(0xFF1E88E5);
  static const Color statusCompletado = Color(0xFF757575);
  static const Color statusCancelado = Color(0xFFE53935);
}
